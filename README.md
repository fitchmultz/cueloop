# ralph-agent-loop has moved to CueLoop

This package is deprecated. The project is now called **CueLoop** and the active Cargo package is `cueloop`.

Install the active package instead:

```bash
cargo install cueloop
```

More info: <https://github.com/fitchmultz/cueloop>
