/**
 WorkspacePerformanceTests

 Responsibilities:
 - Remain the suite-level facade for split workspace performance test support.

 Does not handle:
 - Defining shared support types directly.

 Invariants/assumptions callers must respect:
 - `WorkspacePerformanceTestCase` and `WorkspacePerformanceTestSupport` now live in focused companion files.
 */
