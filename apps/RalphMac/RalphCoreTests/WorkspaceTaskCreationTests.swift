/**
 WorkspaceTaskCreationTests

 Responsibilities:
 - Remain the suite-level facade for split workspace task-creation and queue-watcher regression tests.

 Does not handle:
 - Defining task-creation, queue-refresh, or watcher test classes directly.

 Invariants/assumptions callers must respect:
 - Behavioral coverage now lives in focused companion files plus shared test-support helpers.
 */
