/**
 WindowStateTests

 Responsibilities:
 - Remain the suite-level facade for split window-state persistence and routing regressions.

 Does not handle:
 - Defining window-state test classes directly.

 Invariants/assumptions callers must respect:
 - Persistence/model, restoration/identity, and routing/app-default coverage now live in focused companion files.
 */
