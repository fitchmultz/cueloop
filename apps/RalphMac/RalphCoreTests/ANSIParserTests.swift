/**
 ANSIParserTests

 Responsibilities:
 - Remain the suite-level facade for split ANSI parsing regressions.

 Does not handle:
 - Defining ANSI parser test classes directly.

 Invariants/assumptions callers must respect:
 - Parsing, control-sequence, stream, and edge-case coverage now live in focused companion files.
 */
