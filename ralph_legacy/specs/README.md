# Ralph Legacy Specs

These spec and pin files power the legacy shell and Python tooling in `ralph_legacy/legacy/`.

Use `ralph_legacy/legacy/build_specs.sh` to refresh queue items from the prompt template.
